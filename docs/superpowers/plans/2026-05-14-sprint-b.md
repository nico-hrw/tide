# Sprint B — Calendar Overhaul Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Redesign the mobile calendar — new header layout, all-day event chips, pastel event cards, smooth animations, swipe-down collapse, and infinite scroll.

**Architecture:** All changes in `MobileLayout.tsx`. Three tasks: (1) header, (2) event display, (3) scroll behavior. Each task produces a working state.

**Tech Stack:** React, TypeScript, Framer Motion, Tailwind CSS, date-fns

---

## File Map

| File | Action |
|------|--------|
| `web/src/components/Layout/MobileLayout.tsx` | All changes (3 tasks) |

---

## Task 1: Calendar header redesign

**Files:**
- Modify: `web/src/components/Layout/MobileLayout.tsx`

Read the full current file first.

- [ ] **Step 1: Add isMonthOpen state and touchStartY ref**

Inside `MobileLayout`, after the existing state declarations, add:

```tsx
const [isMonthOpen, setIsMonthOpen] = useState(false);
const touchStartY = useRef<number>(0);
```

Also add `useRef` to the React imports at the top if not already there:
```tsx
import React, { useState, useMemo, useEffect, useRef } from 'react';
```

- [ ] **Step 2: Compute day label**

After the `avatarLetter` line, add:
```tsx
const isDayToday = isSameDay(activeDate, now);
const dayLabel = isDayToday ? 'Today' : format(activeDate, 'EEEE', { locale: enUS });
```

- [ ] **Step 3: Replace the calendar header with new layout**

Find the calendar header block:
```tsx
{activeTab === 'calendar' && (
  <motion.div key={headerKey} {...tabAnim} className="pt-10">
    {/* Collapsible MiniCalendar / week strip */}
    {isCalendarExpanded ? (
      <MiniCalendar selectedDate={activeDate} onSelect={setActiveDate} />
    ) : (
      <div className="flex justify-between items-center px-5 pb-3 pt-1">
        ...
      </div>
    )}
  </motion.div>
)}
```

Replace the ENTIRE block with:

```tsx
{activeTab === 'calendar' && (
  <motion.div key={headerKey} {...tabAnim} className="pt-safe-top pt-8 relative">

    {/* Month overlay — tapping date label toggles this */}
    {isMonthOpen && (
      <div
        className="absolute top-full left-0 right-0 z-50 shadow-lg"
        style={{ backgroundColor: T.card, borderBottom: `1px solid ${T.border}` }}
      >
        <MiniCalendar
          selectedDate={activeDate}
          onSelect={d => { setActiveDate(d); setIsMonthOpen(false); }}
        />
      </div>
    )}

    {isCalendarExpanded ? (
      /* ── Expanded: date label + big day name + week strip ── */
      <div>
        {/* Tappable date label */}
        <button
          onClick={() => setIsMonthOpen(o => !o)}
          className="flex items-center gap-1 px-5 mb-0.5 active:opacity-70"
        >
          {/* TODO(dark): dark:text-gray-500 */}
          <span className="text-xs font-medium" style={{ color: T.textMuted }}>
            {format(activeDate, 'd. MMMM yyyy', { locale: enUS })}
          </span>
          <ChevronRight size={10} className="rotate-90" style={{ color: T.textMuted }} />
        </button>

        {/* Large day name */}
        {/* TODO(dark): dark:text-gray-100 */}
        <p className="text-3xl font-bold px-5 mb-3" style={{ color: T.textPrimary }}>
          {dayLabel}
        </p>

        {/* Week strip */}
        <div className="flex justify-between px-4 pb-3">
          {weekDays.map(d => {
            const isSelected = isSameDay(d, activeDate);
            const isWeekToday = isSameDay(d, now);
            return (
              <button
                key={d.toISOString()}
                onClick={() => setActiveDate(d)}
                className="flex flex-col items-center gap-0.5"
              >
                {/* TODO(dark): dark:text-gray-500 */}
                <span
                  className="text-[10px] font-semibold uppercase"
                  style={{ color: isWeekToday ? T.accent : T.textMuted }}
                >
                  {format(d, 'eeeee', { locale: enUS })}
                </span>
                <span
                  className="text-sm font-bold w-8 h-8 flex items-center justify-center"
                  style={{
                    color: isWeekToday ? T.accent : isSelected ? T.textPrimary : T.textSec,
                    fontWeight: isSelected ? 700 : 500,
                  }}
                >
                  {format(d, 'd')}
                </span>
                {/* Today dot */}
                <div
                  className="w-1 h-1 rounded-full transition-all"
                  style={{ backgroundColor: isWeekToday ? T.accent : 'transparent' }}
                />
              </button>
            );
          })}
        </div>
      </div>
    ) : (
      /* ── Collapsed: compact month label + week strip ── */
      <div className="flex justify-between items-center px-4 pb-3 pt-1">
        <button
          onClick={() => setIsCalendarExpanded(true)}
          className="flex items-center gap-1 active:opacity-70"
          style={{ color: T.textMuted }}
        >
          {/* TODO(dark): dark:text-gray-500 */}
          <span className="text-xs font-semibold uppercase tracking-widest">
            {format(activeDate, 'MMM yyyy', { locale: enUS })}
          </span>
          <ChevronRight size={12} className="rotate-90" style={{ color: T.textMuted }} />
        </button>

        {weekDays.map(d => {
          const isSelected = isSameDay(d, activeDate);
          const isWeekToday = isSameDay(d, now);
          return (
            <button
              key={d.toISOString()}
              onClick={() => setActiveDate(d)}
              className="flex flex-col items-center gap-0.5"
            >
              <span
                className="text-[10px] font-semibold uppercase"
                style={{ color: isWeekToday ? T.accent : T.textMuted }}
              >
                {format(d, 'eeeee', { locale: enUS })}
              </span>
              <span
                className="text-sm w-8 h-8 flex items-center justify-center"
                style={{
                  color: isWeekToday ? T.accent : isSelected ? T.textPrimary : T.textSec,
                  fontWeight: isSelected ? 700 : 400,
                }}
              >
                {format(d, 'd')}
              </span>
              <div
                className="w-1 h-1 rounded-full"
                style={{ backgroundColor: isWeekToday ? T.accent : 'transparent' }}
              />
            </button>
          );
        })}
      </div>
    )}
  </motion.div>
)}
```

- [ ] **Step 4: Wrap the header container in motion.div with layout for smooth resize**

Find the header container div:
```tsx
<div
  className="w-full z-40 shrink-0"
  style={{ backgroundColor: T.card, borderBottom: `1px solid ${T.border}` }}
>
```

Replace with `motion.div` with layout:
```tsx
<motion.div
  layout
  transition={{ duration: 0.25, ease: 'easeInOut' }}
  className="w-full z-40 shrink-0"
  style={{ backgroundColor: T.card, borderBottom: `1px solid ${T.border}` }}
>
```

Also update the closing `</div>` to `</motion.div>`.

- [ ] **Step 5: Verify TypeScript**

```bash
cd web && npx tsc --noEmit
```

Expected: no output.

---

## Task 2: All-day event chips + pastel event cards + progress line fix

**Files:**
- Modify: `web/src/components/Layout/MobileLayout.tsx`

Read the current file first.

- [ ] **Step 1: Split dayEvents into allDay and timed inside upcomingDays.map**

Inside the `upcomingDays.map(({ date, events: dayEvents }, dayIdx) => {` block, at the very top before the `isDayToday` line, add:

```tsx
const allDayEvents = dayEvents.filter(e =>
  e.allDay === true ||
  (format(new Date(e.start), 'HH:mm') === '00:00' &&
   e.end && format(new Date(e.end), 'HH:mm') === '23:59')
);
const timedEvents = dayEvents.filter(e => !allDayEvents.includes(e));
```

- [ ] **Step 2: Update progress line to use timedEvents only**

Find:
```tsx
if (isDayToday && dayEvents.length >= 2) {
  const firstStart = new Date(dayEvents[0].start).getTime();
  const lastStart = new Date(dayEvents[dayEvents.length - 1].start).getTime();
```

Replace with:
```tsx
if (isDayToday && timedEvents.length >= 2) {
  const firstStart = new Date(timedEvents[0].start).getTime();
  const lastStart = new Date(timedEvents[timedEvents.length - 1].start).getTime();
```

- [ ] **Step 3: Render all-day chips after date separator, before timeline**

After the date separator `<div className="flex items-center gap-4 mb-5">...</div>`, add the all-day chips section:

```tsx
{/* All-day event chips */}
{allDayEvents.length > 0 && (
  <div className="flex flex-wrap gap-2 mb-4">
    {allDayEvents.map(event => (
      <div
        key={event.id}
        className="flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-semibold"
        style={{
          backgroundColor: (event.color || T.accent) + '25',
          color: event.color || T.accent,
        }}
      >
        <div
          className="w-1.5 h-1.5 rounded-full shrink-0"
          style={{ backgroundColor: event.color || T.accent }}
        />
        {event.title || 'All Day Event'}
      </div>
    ))}
  </div>
)}
```

- [ ] **Step 4: Change timeline to use timedEvents**

Find the timeline render:
```tsx
{dayEvents.length === 0 ? (
  <p className="text-xs py-2 mb-2" style={{ color: T.textMuted }}>
    No events
  </p>
) : (
  <div className="relative flex flex-col gap-6">
    {/* Vertical timeline line */}
    ...
    {dayEvents.map((event, idx) => {
```

Replace the condition and inner map:
```tsx
{timedEvents.length === 0 && allDayEvents.length === 0 ? (
  <p className="text-xs py-2 mb-2" style={{ color: T.textMuted }}>
    No events
  </p>
) : timedEvents.length === 0 ? null : (
  <div className="relative flex flex-col gap-6">
    {/* Vertical timeline line */}
    <div
      className="absolute top-2 bottom-4 w-0.5 z-0"
      style={{ left: '4rem', background: lineBackground }}
    />

    {timedEvents.map((event, idx) => {
```

- [ ] **Step 5: Update event card to always show pastel background + hollow dot**

Inside `timedEvents.map`, replace the dot and content div styling.

**Current dot:**
```tsx
<div
  className="rounded-full absolute ring-4 ring-white z-10 transition-all"
  style={{
    backgroundColor: dotColor,
    width: isActive ? '14px' : '10px',
    height: isActive ? '14px' : '10px',
    top: isActive ? '0px' : '2px',
    left: isActive ? '-7px' : '-5px',
  }}
/>
```

**Replace with hollow ring for past/future, solid for active:**
```tsx
<div
  className="rounded-full absolute ring-4 ring-white z-10 transition-all"
  style={{
    backgroundColor: isActive ? (event.color || T.accent) : 'white',
    border: `2px solid ${event.color || T.accent}`,
    width: isActive ? '14px' : '10px',
    height: isActive ? '14px' : '10px',
    top: isActive ? '0px' : '2px',
    left: isActive ? '-7px' : '-5px',
    opacity: isPast ? 0.5 : 1,
  }}
/>
```

**Current content div:**
```tsx
<div
  className="pl-4 pb-1 rounded-2xl transition-all"
  style={
    isExpanded
      ? {
          backgroundColor: (event.color || T.accent) + '15',
          outline: `1px solid ${event.color || T.accent}33`,
          padding: '8px 8px 8px 16px',
          marginTop: '-2px',
        }
      : {}
  }
>
```

**Replace with always-on pastel card:**
```tsx
<div
  className="pl-4 pb-2 pt-2 pr-3 rounded-2xl transition-all ml-1"
  style={{
    backgroundColor: (event.color || T.accent) + (isExpanded ? '28' : '15'),
    borderLeft: `3px solid ${event.color || T.accent}${isPast ? '80' : 'ff'}`,
  }}
>
```

- [ ] **Step 6: Verify TypeScript**

```bash
cd web && npx tsc --noEmit
```

Expected: no output.

---

## Task 3: Scroll behavior — smooth collapse, swipe-down, infinite scroll

**Files:**
- Modify: `web/src/components/Layout/MobileLayout.tsx`

Read the current file first.

- [ ] **Step 1: Add visibleDays state**

Inside `MobileLayout`, after the `now` state:
```tsx
const [visibleDays, setVisibleDays] = useState(5);
```

- [ ] **Step 2: Reset visibleDays when activeDate changes**

Find the existing `useEffect` that resets `expandedEventId` on `activeDate` change:
```tsx
useEffect(() => {
  setExpandedEventId(null);
}, [activeDate]);
```

Add `visibleDays` reset to the same effect:
```tsx
useEffect(() => {
  setExpandedEventId(null);
  setVisibleDays(5);
}, [activeDate]);
```

- [ ] **Step 3: Update upcomingDays to use visibleDays**

Find:
```tsx
const upcomingDays = useMemo(() => {
  return Array.from({ length: 5 }).map((_, i) => {
```

Replace `5` with `visibleDays` and add it to the dependency array:
```tsx
const upcomingDays = useMemo(() => {
  return Array.from({ length: visibleDays }).map((_, i) => {
    const date = addDays(activeDate, i);
    return { date, events: filterEventsForDate(events, date) };
  });
}, [events, activeDate, visibleDays]);
```

- [ ] **Step 4: Update handleScroll to collapse calendar and load more days**

Find the current `handleScroll`:
```tsx
const handleScroll = (e: React.UIEvent<HTMLDivElement>) => {
  if (activeTab !== 'calendar') return;
  if (e.currentTarget.scrollTop > 10) {
    setIsCalendarExpanded(false);
  }
};
```

Replace with:
```tsx
const handleScroll = (e: React.UIEvent<HTMLDivElement>) => {
  if (activeTab !== 'calendar') return;
  const el = e.currentTarget;

  // Collapse calendar header on scroll down
  if (el.scrollTop > 10) {
    setIsCalendarExpanded(false);
  }

  // Infinite scroll: load 5 more days when within 300px of bottom
  if (el.scrollHeight - el.scrollTop - el.clientHeight < 300) {
    setVisibleDays(d => d + 5);
  }
};
```

- [ ] **Step 5: Add touch handlers for swipe-down re-expand**

On the scrollable content div (`<div className="flex-1 w-full overflow-y-auto no-scrollbar pb-16" onScroll={handleScroll}>`), add touch handlers:

```tsx
<div
  className="flex-1 w-full overflow-y-auto no-scrollbar pb-16"
  onScroll={handleScroll}
  onTouchStart={e => { touchStartY.current = e.touches[0].clientY; }}
  onTouchMove={e => {
    if (activeTab !== 'calendar' || isCalendarExpanded) return;
    if (e.currentTarget.scrollTop > 0) return;
    const dy = e.touches[0].clientY - touchStartY.current;
    if (dy > 40) setIsCalendarExpanded(true);
  }}
>
```

- [ ] **Step 6: Verify TypeScript**

```bash
cd web && npx tsc --noEmit
```

Expected: no output.

---

## Self-Review

**Spec coverage:**
- ✅ B1 Header (date label + day name + week strip) → Task 1 Step 3
- ✅ Today in accent text color, dot below, no filled circle → Task 1 Step 3
- ✅ MiniCalendar as dropdown on date tap → Task 1 Step 3 (isMonthOpen)
- ✅ Smooth collapse animation (motion.div layout) → Task 1 Step 4
- ✅ B2 All-day chips → Task 2 Steps 1, 3
- ✅ B3 Always-on pastel card + hollow dot → Task 2 Step 5
- ✅ B4 Progress line uses timedEvents only → Task 2 Step 2
- ✅ B5 layout animation → Task 1 Step 4
- ✅ B6 Swipe-down re-expand → Task 3 Step 5
- ✅ B7 Infinite scroll → Task 3 Steps 1-4

**Type consistency:**
- `visibleDays: number` state used in `upcomingDays` memo and reset in `activeDate` effect ✅
- `touchStartY: React.MutableRefObject<number>` used in onTouchStart/onTouchMove ✅
- `allDayEvents` and `timedEvents` scoped inside `upcomingDays.map` ✅
- `isMonthOpen: boolean` used in header conditional ✅
