# AI INSTRUCTIONS

**CRITICAL DIRECTIVE:** 
EVERY TIME an AI assistant starts working on this project, it MUST first read this file and the latest architecture files. 

For EVERY modification made, the AI MUST create a new sequentially numbered file in the `changelogs/` folder (e.g., `001_initial_setup.md`, `002_social_bonds.md`) detailing:
1. Exactly what files were changed.
2. The database schema updates (if any).
3. The precise technical reasoning behind the changes.

**CONTINUOUS DOCUMENTATION RULE:**
EVERY time an AI works on this codebase, it MUST iteratively improve or expand the architecture docs (e.g. `SYSTEM_FLOW.md` or `DATABASE.md`) by roughly 10 meaningful lines to keep them evolving alongside the codebase.

Failure to follow these instructions will result in compromised project architecture and is considered a critical error.
